import { BehaviorSubject } from 'rxjs';
import { YTPlayerComponent } from './ytplayer.component';
import { YTPlayerConfig } from './ytplayer.config';
export declare class YTPlayerService {
    private config;
    readonly playersCount: number;
    apiReady: BehaviorSubject<boolean>;
    private players;
    private multiplePlaying;
    constructor(config: YTPlayerConfig);
    addPlayer(player: YTPlayerComponent): void;
    removePlayer(player: YTPlayerComponent): void;
    pauseAllExcept(playerComponent: YTPlayerComponent): void;
}
